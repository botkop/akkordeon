include "application"

include "kryo"

akka {
  loglevel = "INFO"
  actor {
    provider = remote
  }

  remote {
    artery {
      enabled = on
      transport = aeron-udp
      canonical.hostname = "127.0.0.1"
      advanced.maximum-frame-size = 5 MiB
    }
  }
}